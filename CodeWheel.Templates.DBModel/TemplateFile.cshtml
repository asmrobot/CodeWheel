@model CodeWheel.Templates.DBModel.DataEntity
@Model.ImportNameSpace

namespace @Model.NameSpace
{
    public partial class @(Model.ClassPre + Model.CurrentTable.TableName)
    {
        @foreach (var col in Model.CurrentTable.Columns)
        {
            <text>
            public @(col.FieldTypeName) @(col.ColumnName)
            {
                get;
                set;
            }
            </text>

        }

    } 
}